import { BrowserRouter, Routes, Route } from "react-router-dom";
const Home = () => {
    return <h1>React, sometimes referred to as a frontend JavaScript framework, is a JavaScript library created by Facebook.

    React is a tool for building UI components.</h1>;
  };
  
  export default Home;